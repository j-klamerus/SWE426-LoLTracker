<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".ui.RecentFragment">

    <data>
        <variable
            name="viewModel"
            type="com.example.loltracker.ui.RecentViewModel"/>
    </data>

    <ScrollView
        android:id="@+id/recentScroll"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="30dp"
        android:layout_marginBottom="30dp">

        <!-- Adding new favorite -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginLeft="30dp"
            android:layout_marginRight="30dp"
            android:orientation="vertical">

            <TextView
                android:id="@+id/recentDisplayText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@{viewModel.recentString}" />

            <Button
                android:id="@+id/recentDeleteButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Clear Recent Searches"
                android:onClick="@{() -> viewModel.delRecent()}"/>

        </LinearLayout>

        <!-- Potential linear layout / buttons go here -->

    </ScrollView>

</layout>